// Canonical Candera Candles Etsy shop ID, used as the fallback wherever
// ETSY_SHOP_ID is unset or invalid. Kept as a number to match every call site
// (Etsy's API and the sync engine treat shop IDs as numeric).
export const ETSY_SHOP_ID_FALLBACK = 25894791
