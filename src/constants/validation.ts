/** Shared email validation regex used across all storefront forms. */
export const EMAIL_PATTERN = /^\S[^\s@]*@\S+$/

/** Submission size limits shared by the client schema and server-side sanitizer. */
export const MAX_FIELDS = 50
export const MAX_VALUE_LENGTH = 5000
export const MAX_FIELD_NAME_LENGTH = 200
