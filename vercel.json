{
  "buildCommand": "echo y | NODE_OPTIONS=--no-deprecation payload migrate && NODE_OPTIONS=--no-deprecation npx tsx scripts/seed-admin.ts && pnpm build",
  "installCommand": "pnpm install",
  "crons": [
    {
      "path": "/api/payload-jobs/run",
      "schedule": "0 0 * * *"
    }
  ]
}
