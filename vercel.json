{
  "buildCommand": "pnpm run ci",
  "installCommand": "rm -rf node_modules && pnpm install",
  "ignoreCommand": "echo \"$VERCEL_GIT_COMMIT_REF\" | grep -qE '^dependabot/'",
  "crons": [
    {
      "path": "/api/payload-jobs/run",
      "schedule": "0 0 * * *"
    }
  ]
}
