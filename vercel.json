{
  "buildCommand": "NODE_OPTIONS=--no-deprecation echo y | payload migrate && npx tsx scripts/seed-admin.ts && pnpm build",
  "installCommand": "pnpm install",
  "crons": [
    {
      "path": "/api/payload-jobs/run",
      "schedule": "0 0 * * *"
    }
  ]
}
